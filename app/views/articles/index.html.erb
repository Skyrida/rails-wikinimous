<h1>WikiLeaks - Capital City Edition</h1>

<ul>
  <% @articles.each do | article | %>
    <li><%= link_to article.title, article_path(article) %> <%= link_to "Delete", article_path(article), data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete #{article.title.upcase}?"} %></li>
  <% end %>
</ul>

<p><%= link_to "Create new article", new_article_path %></p>
